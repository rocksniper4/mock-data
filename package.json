{
  "name": "new",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npx json-server --watch db.json --port 8000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.2"
  },
  "engines": {
      "node": ">=16.0.0"
    }
}
